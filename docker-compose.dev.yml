# Development Environment Configuration
# Uses volumes to mount local code for hot-reloading.
services:
  db:
    image: pgvector/pgvector:pg17
    container_name: db_dev
    volumes:
      - postgres_dev_data:/var/lib/postgresql/data/
    ports:
      - "5432:5432" # Expose PostgreSQL port for local debugging if needed
    env_file:
      - .env.dev
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready" ]
      interval: 5s
      timeout: 5s
      retries: 5

  processor:
    container_name: processor_dev
    build:
      context: processor
      target: dev # Use the 'dev' stage from the processor Dockerfile
    volumes:
      - ./processor:/processor # Mount local 'processor' folder into the container
      - /processor/.venv # Don't mount local .venv, use the one in the container
    ports:
      - "8000:8000"
    env_file:
      - .env.dev
    depends_on:
      db:
        condition: service_healthy

volumes:
  postgres_dev_data: